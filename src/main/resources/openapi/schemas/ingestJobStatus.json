{
  "description": "Ingest job information",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "description" : "job identifier",
      "format": "uuid"
    },
    "numberOfRecords": {
      "type": "integer",
      "description" : "number of records in job so far"
    },
    "elapsedTime": {
      "type": "string",
      "description" : "Elapsed time since job was created"
    }
  },
  "additionalProperties": true,
  "required": [
    "jobId"
  ]
}
